# Database Configuration
# SQLite for dev/ALU deployment (no external dependencies)
DATABASE_URL=sqlite:///./data/strokelink.db

# Security
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secure-key-here-change-in-production

# Firebase (Auth only)
FIREBASE_KEY_PATH=firebase-key.json

# Optional: Google Drive API credentials
GOOGLE_DRIVE_CREDENTIALS_PATH=google-drive-credentials.json
